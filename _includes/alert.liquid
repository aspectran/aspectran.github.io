{% comment %}
*
* This include lets you easily display an alert. To use this include no `.html` ending is necessary.
*
* You can use the following kinds of alerts (mapped to Bootstrap alert types):
*
*  - primary
*  - secondary
*  - success
*  - danger
*  - warning
*  - info
*  - light
*  - dark
*
* An icon is automatically added based on the alert type (success, danger, warning, info).
*
* Legacy types from Foundation are mapped as follows:
*  - alert: mapped to 'danger'
*  - text: mapped to 'secondary'
*  - terminal: mapped to 'dark'
*
* You can even use markdown and <html>-tags inside the alert, but beware to use " and ' properly; for example:
*
*     {% include alert.liquid info='Show an info alert with <b>outstanding</b> <a href="#info">information</a>.' %}
*
*     {% include alert.liquid info='Show an info alert with *outstanding* [information](#info).' %}
*
*     {% include alert.liquid info='Show an info alert with *outstanding* <a href="#info">information</a>.' %}
*
* You can also call for additional classes to be put on the alert div:
*
*     {% include alert.liquid success="Yay! you did it!" classes='custom-class' %}
*
{% endcomment %}
{% for entry in include %}
{% if entry[0] == 'classes' %}{% continue %}{% endif %}
{% assign type = entry[0] %}
{% assign icon = '' %}
{% if type == 'alert' %}
  {% assign type = 'danger' %}
{% elsif type == 'text' %}
  {% assign type = 'secondary' %}
{% elsif type == 'terminal' %}
  {% assign type = 'dark terminal' %}
{% endif %}
{% case type %}
  {% when 'success' %}
    {% assign icon = '<i class="bi bi-check-circle fs-3 pe-3"></i>' %}
  {% when 'danger' %}
    {% assign icon = '<i class="bi bi-check-circle fs-3 pe-3"></i>️' %}
  {% when 'warning' %}
    {% assign icon = '<i class="bi bi-exclamation-triangle fs-3 pe-3"></i>' %}
  {% when 'info' %}
    {% assign icon = '<i class="bi bi-info-circle fs-3 pe-3"></i>' %}
{% endcase %}
<div class="alert alert-{{ type }} d-flex align-items-center py-2 {{ include.classes }}" role="alert">
  {{ icon }}
  {{ entry[1] | strip | markdownify }}
</div>
{% endfor %}
